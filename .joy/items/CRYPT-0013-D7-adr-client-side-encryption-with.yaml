id: CRYPT-0013-D7
title: 'ADR: Client-side encryption with cleartext metadata'
type: decision
status: closed
priority: medium
capabilities:
- conceive
validity: replaced
replaced_by: CRYPT-000F-B2
created_by: ai:claude@joy delegated-by:horst.schwarz@joydev.com
created: 2026-06-15T06:38:09.364504Z
updated: 2026-06-15T06:38:09.526164Z
updated_by: ai:claude@joy delegated-by:horst.schwarz@joydev.com
history:
- date: 2026-06-15T06:38:09.442387Z
  by: ai:claude@joy delegated-by:horst.schwarz@joydev.com
- date: 2026-06-15T06:38:09.526164Z
  by: ai:claude@joy delegated-by:horst.schwarz@joydev.com
description: |-
  **Status:** Superseded (2026-05) by ADR-038: Selective encryption with optional whole-project mode (JI-011B-08)

  The whole-project E2E default specified below is no longer the model. Encryption in Joy is selective by default; whole-project encryption is available as an explicit `--all` choice. See ADR-038 and [`docs/dev/vision/guardianship/Crypt.md`](https://docs.int.joydev.com/doc/crypt-selective-encryption-for-joy-dqDnSlnAqC) for the current design.

  ## Context

  Data on joyint.com must be E2E-encrypted. The server should never have access to item content. At the same time, server-side features (notifications, CalDAV scheduling, dashboards) need some visibility into item metadata.

  ## Decision

  All data on joyint.com is always E2E-encrypted (AES-256-GCM). The encryption key stays on the client device.

  **Encrypted:** title, description, comments, custom fields -- everything that constitutes item content.

  **Cleartext metadata:** id, status, priority, due_date, timestamps, project name. Required for server-side notifications, CalDAV VTODO scheduling, and overview dashboards.

  **Decryption by interface:**

  * CLI decrypts locally
  * WebUI decrypts client-side in the browser (Web Crypto API)
  * CalDAV is a conscious opt-in: the user authorizes joyint.com to decrypt for CalDAV delivery (e.g. via `jyn key share`). Without opt-in, no CalDAV -- only CLI and WebUI

  **Key management:** Per-project key stored in OS keychain (via `keyring` crate) with fallback to `~/.config/joy/keys/{project-id}.key`. Key generation via `joy key init` or `jyn key init`.

  ## Consequences

  The server stores only encrypted blobs plus cleartext metadata. No access to sensitive content. CalDAV integration requires explicit trust delegation from the user -- privacy is the default, convenience is opt-in.

  Trade-off: full-text search requires client-side decryption (slower for large projects). Cleartext metadata exposes structure (how many items, their status distribution) but not content.

  (Relocated from umbrella JI-00FB-95 as part of the JI-0120 architecture reconciliation.)
